namespace Task1
{
    internal class Program
    {
        //    enum DayOfWeek { MON, TUE, WED, THU, FRI, SAT, SUN };

        //    //the function outputs the names of the days
        //    static void PrintDayOfWeek(DayOfWeek dow)
        //    {
        //        switch (dow)
        //        {



        //        }
        //        //: here you have to create SWITCH statement to check the day 
        //        //the hint: if the name of the day is MON, so to Print "Monday", etc.
        //    }

        //    //the function inputs the days of week (from 1 to 7) and returns the result of calling the function DayOfWeek with inputted argument
        //    static DayOfWeek ReadDayOfWeek()
        //    {
        //        int day;
        //        day = int.Parse(Console.ReadLine());

        //        //if ( /*: print the condition when an exception occurs*/ )
        //        //    throw new ArgumentOutOfRangeException("day", "day must be > 0 and <8; day=" + day.ToString());
        //        //return /*: here it is needed to make a conversion of the type*/ day;
        //    }
        //    static void Main()
        //    {
        //        DayOfWeek dow;  /*: with a help of the function ReadDayOfWeek you have to initialize dow*/

        //        //: call the function PrintDayOfWeek

        //    }
    }
}